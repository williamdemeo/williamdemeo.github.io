__pycache__/
*.py[cod]
.venv/
site/
.DS_Store

# Nix: `nix build` output symlinks and the direnv shell cache.
result
result-*
.direnv/
node_modules/

# Upstream font sources, fetched by `make fonts` and content-addressed.
.fonts-cache/
